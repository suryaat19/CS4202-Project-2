# Results

One folder per run: `runs/<YYYYMMDD>-<model>-<track>/` with `manifest.json`
(model id + version, date, git SHA, dataset hash, prompt hash, decoding params, seed),
`outputs.jsonl`, and `scores.json`.
